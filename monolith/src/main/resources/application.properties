spring.jpa.open-in-view=false
server.port=8080
use.confirmation.service=false
confirmationms.url=http://localhost:8070/

# Strangler toggle for the extracted counterparty credit-check capability.
# false (default) => in-process LocalCreditService (original monolith behavior)
# true            => RemoteCreditService routing to the credit-service below
rfq.credit.service.remote=false
rfq.credit.service.url=http://localhost:8060/
